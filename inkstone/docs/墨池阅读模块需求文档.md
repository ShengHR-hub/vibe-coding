# 墨池 Inkstone · 阅读模块需求文档

> 版本：v2.0  日期：2026-06-14  状态：需求确认

---

## 一、项目背景

墨池（Inkstone）是 AI 驱动的全品类写作社交平台，当前已完成 **17 个蓝图、103 个端点、21 条路由、25 张表、50+ Vue 组件**。现计划新增**阅读模块**，使墨池成为"读写一体"的完整创作生态。

**核心理念**：写作时通过阅读积累经验和灵感，阅读时通过写作功能缓解疲劳和激发创作。读与写相互滋养、深度融合。

**现有基础**：Reader.vue 已有基础阅读能力（章节目录、字体大小、评论、点赞、收藏、导出、分享），本次在此基础上扩展为完整的阅读平台。

---

## 二、模块总览

| 维度 | 目标 |
|------|------|
| 定位 | 完整阅读平台 = 沉浸式阅读器 + 数字书库 + 阅读社交 |
| 内容来源 | 平台内用户创作作品（works 表）+ 外部导入书籍（library_books 表） |
| 内容类型 | 小说、散文、诗歌、网文 |
| 视觉风格 | SingularityBackground 灰阶背景（speed=0.5, brightness=0.6, saturation=0）+ 毛玻璃卡片 + 金色点缀 |
| 规模 | 8 个蓝图、~55 个端点、8 个页面、10 张新表 |
| 实施策略 | 按功能链条推进：导入 → 书库 → 书架 → 阅读器 → 批注 → 打卡 → 报告 |

---

## 三、与现有系统的关系

### 3.1 复用的表

| 表 | 复用方式 |
|----|----------|
| `works` | 平台作品通过 `status='published'` 在书库中展示 |
| `chapters` | 平台作品章节，阅读器直接读取 |
| `users` | 用户信息，书架/批注/打卡的外键目标 |
| `materials` | 写作素材库，好句标记同步目标 |
| `achievements` / `user_achievements` | 新增阅读类成就定义 |

### 3.2 复用的端点

| 端点 | 复用方式 |
|------|----------|
| `GET /api/works/public/:id` | 书籍详情页读取平台作品信息 |
| `GET /api/works/public/:id` | 阅读器读取平台作品章节 |
| `POST /api/interactions/like` | 书评区点赞复用 |
| `POST /api/interactions/favorite` | 书籍收藏复用现有收藏表或新建书架表 |

### 3.3 不复用、新建的部分

| 类别 | 新建内容 |
|------|----------|
| 数据库 | 10 张新表（library_books, library_chapters, library_volumes, reading_bookshelf, reading_progress, reading_bookmarks, reading_annotations, reading_reviews, reading_checkins, reading_highlights） |
| 后端蓝图 | 8 个新蓝图（library, bookshelf, reading, bookmarks, annotations, reviews, checkins, highlights） |
| 前端页面 | 8 个新页面 |

---

## 四、功能模块详细设计

### 模块 1：书库系统

统一管理平台内作品和外部导入书籍，提供分类、搜索、排行榜。

#### 1.1 书库首页 `/library`

| 功能 | 说明 |
|------|------|
| 精选推荐 | 编辑精选 / AI 推荐 / 热门榜 / 新书榜 |
| 分类浏览 | 按类型（小说/散文/诗歌/网文）、按标签、按字数范围 |
| 全文搜索 | 书名、作者、内容全文检索 |
| 排行榜 | 热度榜、收藏榜、评分榜、完本榜、连载榜 |
| 数据来源 | `works`（status=published）+ `library_books` 合并查询 |

#### 1.2 书籍详情页 `/library/:type/:id`

| 功能 | 说明 |
|------|------|
| 基本信息 | 书名、作者、简介、标签、字数、章节数、连载状态 |
| 数据展示 | 阅读量、收藏数、评分、评论数 |
| 目录 | 按卷/章展示，标注已读进度 |
| 操作入口 | 开始阅读、加入书架、分享、举报 |
| 书评区 | 长评 + 短评，支持点赞和回复 |
| 相似推荐 | AI 推荐同类型作品 |

> `:type` 为 `work` 或 `library`，区分平台作品和导入书籍。

#### 1.3 书籍导入 `/library/upload`

| 功能 | 说明 |
|------|------|
| 前端用户上传 | 登录用户可上传个人 TXT 文件，自动解析章节、提取元信息 |
| 后台批量导入 | 管理员通过脚本批量导入（复用/增强 import_novel.py） |
| 格式支持 | TXT（UTF-8/GBK/GB18030 自动检测编码） |
| 解析规则 | 自动识别书名、作者、章节标题（`第X章`、`书名：` 等格式） |
| 去重检测 | 同名同作者的书籍提示已存在 |

#### 1.4 数据库表

```sql
CREATE TABLE library_books (
    book_id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    author VARCHAR(100) NOT NULL,
    summary TEXT,
    cover_image VARCHAR(500) DEFAULT '',
    type ENUM('novel', 'essay', 'poetry', 'webfiction') DEFAULT 'novel',
    tags VARCHAR(500) DEFAULT '',
    word_count INT DEFAULT 0,
    chapter_count INT DEFAULT 0,
    serialization_status ENUM('serializing', 'completed', 'paused') DEFAULT 'completed',
    source VARCHAR(100) DEFAULT '',
    uploader_id INT DEFAULT NULL,
    views INT DEFAULT 0,
    favorites_count INT DEFAULT 0,
    rating_avg DECIMAL(3,2) DEFAULT 0,
    rating_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (uploader_id) REFERENCES users(user_id) ON DELETE SET NULL,
    INDEX idx_book_type (type),
    INDEX idx_book_created (created_at)
);

CREATE TABLE library_chapters (
    chapter_id INT AUTO_INCREMENT PRIMARY KEY,
    book_id INT NOT NULL,
    volume_id INT DEFAULT NULL,
    chapter_no INT NOT NULL,
    title VARCHAR(200) DEFAULT '',
    content LONGTEXT,
    word_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (book_id) REFERENCES library_books(book_id) ON DELETE CASCADE,
    INDEX idx_lch_book (book_id)
);

CREATE TABLE library_volumes (
    volume_id INT AUTO_INCREMENT PRIMARY KEY,
    book_id INT NOT NULL,
    volume_no INT NOT NULL,
    title VARCHAR(200) DEFAULT '',
    summary TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (book_id) REFERENCES library_books(book_id) ON DELETE CASCADE
);
```

#### 1.5 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/library | 书库列表（分页、筛选、排序） | 否 |
| GET | /api/library/search?q=&type=&sort= | 全文搜索 | 否 |
| GET | /api/library/categories | 分类标签列表 | 否 |
| GET | /api/library/rankings?sort=hot\|rating\|new | 排行榜 | 否 |
| GET | /api/library/:id | 书籍详情 | 否 |
| GET | /api/library/:id/chapters | 章节列表 | 否 |
| GET | /api/library/:id/chapters/:cid | 章节内容 | 否 |
| POST | /api/library/upload | 用户上传 TXT | 是 |
| GET | /api/library/featured | 精选推荐 | 否 |

---

### 模块 2：个人书架

用户管理自己的阅读记录和书籍收藏。

#### 2.1 书架页面 `/bookshelf`

| 功能 | 说明 |
|------|------|
| 在读 | 有阅读进度但未读完的书籍，显示进度百分比 |
| 已读 | 用户标记为"已读完"的书籍 |
| 想读 | 收藏但尚未开始阅读的书籍 |
| 自定义书单 | 用户创建自定义分类（如"仙侠必读"、"写作参考"） |
| 排序筛选 | 按最近阅读、加入时间、评分、字数排序 |
| 批量管理 | 批量移动分组、批量删除 |

#### 2.2 数据库表

```sql
CREATE TABLE reading_bookshelf (
    shelf_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    shelf_group ENUM('reading', 'finished', 'want_read') DEFAULT 'want_read',
    custom_folder VARCHAR(100) DEFAULT '',
    rating TINYINT DEFAULT 0,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    UNIQUE KEY uk_bookshelf (user_id, book_type, book_id),
    INDEX idx_shelf_user (user_id, shelf_group)
);
```

#### 2.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/bookshelf?group=reading\|finished\|want_read | 书架列表 | 是 |
| POST | /api/bookshelf | 加入书架 | 是 |
| PUT | /api/bookshelf/:id | 更新分组/评分/书单 | 是 |
| DELETE | /api/bookshelf/:id | 从书架移除 | 是 |
| GET | /api/bookshelf/folders | 自定义书单列表 | 是 |
| POST | /api/bookshelf/folders | 创建书单 | 是 |

---

### 模块 3：沉浸式阅读器

在现有 Reader.vue 基础上升级为沉浸式阅读体验。支持平台作品和导入书籍。

#### 3.1 阅读界面

| 功能 | 说明 |
|------|------|
| 滚动阅读 | 上下连续滚动，类似微信读书默认模式 |
| 顶部工具栏 | 返回、目录、设置、批注、分享 |
| 底部信息栏 | 当前章节、阅读进度百分比 |
| 章节切换 | 顶部下拉章节列表（复用 Reader.vue 现有样式，按卷分组） |
| 自动隐藏 | 阅读时工具栏自动隐藏，点击屏幕中央唤出 |
| 背景 | SingularityBackground 灰阶效果（speed=0.5, brightness=0.6, saturation=0） |

#### 3.2 阅读设置

| 功能 | 说明 |
|------|------|
| 字体大小 | 12px ~ 28px 滑块调节 |
| 行间距 | 1.5 / 1.8 / 2.0 / 2.5 四档 |
| 字体选择 | 系统默认 / 宋体 / 楷体 / 黑体 |
| 背景主题 | 默认暗色 / 护眼绿 / 羊皮纸 / 纯白 / 纯黑 |
| 亮度调节 | 软件级亮度调节（CSS filter） |
| 翻页方式 | 滚动（默认） |
| 设置持久化 | 所有设置存入 localStorage，全局生效 |

#### 3.3 阅读进度

| 功能 | 说明 |
|------|------|
| 自动记录 | 每 10 秒自动保存当前阅读位置（章节 + 段落偏移） |
| 跨设备同步 | 登录状态下进度存服务端，换设备可恢复 |
| 进度展示 | 书架显示每本书的阅读百分比 |
| 续读提示 | 打开已读过的书，提示"上次读到第X章，是否继续" |

#### 3.4 数据库表

```sql
CREATE TABLE reading_progress (
    progress_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    chapter_id INT NOT NULL,
    scroll_percent DECIMAL(5,2) DEFAULT 0,
    total_percent DECIMAL(5,2) DEFAULT 0,
    last_read_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    UNIQUE KEY uk_progress (user_id, book_type, book_id)
);
```

#### 3.5 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/reading/progress/:type/:id | 获取阅读进度 | 是 |
| PUT | /api/reading/progress/:type/:id | 更新阅读进度 | 是 |
| GET | /api/reading/history | 阅读历史列表 | 是 |

#### 3.6 路由设计

```
/reader/:type/:id    — type=work 或 library，统一阅读器入口
```

---

### 模块 4：书签系统

标记精彩段落，快速跳转。

#### 4.1 功能

| 功能 | 说明 |
|------|------|
| 添加书签 | 选中段落或点击侧边栏添加书签 |
| 书签列表 | 按书籍分组展示所有书签 |
| 快速跳转 | 点击书签直接滚动到对应位置 |
| 书签备注 | 给书签添加简短备注 |
| 导出书签 | 导出为文本文件 |

#### 4.2 数据库表

```sql
CREATE TABLE reading_bookmarks (
    bookmark_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    chapter_id INT NOT NULL,
    paragraph_idx INT DEFAULT 0,
    selected_text VARCHAR(500),
    note VARCHAR(500) DEFAULT '',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    INDEX idx_bm_user (user_id, book_type, book_id)
);
```

#### 4.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/bookmarks?book_type=&book_id= | 书签列表 | 是 |
| POST | /api/bookmarks | 添加书签 | 是 |
| DELETE | /api/bookmarks/:id | 删除书签 | 是 |

---

### 模块 5：段落批注系统

对具体段落写笔记，可选公开或私密。

#### 5.1 功能

| 功能 | 说明 |
|------|------|
| 创建批注 | 选中文字 → 弹出批注输入框 |
| 批注标记 | 有批注的段落侧边显示标记点 |
| 可见性 | 每条批注可选"仅自己可见"或"公开" |
| 公开批注 | 其他用户可以看到公开批注并回复讨论 |
| 批注面板 | 侧边栏展示当前章节的所有公开批注 |
| 我的批注 | `/annotations` 个人中心查看自己所有批注 |
| 批注统计 | 一本书共有多少条公开批注 |

#### 5.2 数据库表

```sql
CREATE TABLE reading_annotations (
    annotation_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    chapter_id INT NOT NULL,
    paragraph_idx INT DEFAULT 0,
    selected_text VARCHAR(500),
    content TEXT NOT NULL,
    is_public BOOLEAN DEFAULT FALSE,
    parent_id INT DEFAULT NULL,
    likes_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    FOREIGN KEY (parent_id) REFERENCES reading_annotations(annotation_id) ON DELETE CASCADE,
    INDEX idx_ann_book (book_type, book_id, chapter_id),
    INDEX idx_ann_user (user_id)
);
```

#### 5.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/annotations/:type/:id/:chapter_id | 获取章节批注 | 否（仅返回公开的） |
| POST | /api/annotations | 创建批注 | 是 |
| PUT | /api/annotations/:id | 编辑/修改公开性 | 是 |
| DELETE | /api/annotations/:id | 删除批注 | 是 |
| POST | /api/annotations/:id/reply | 回复批注 | 是 |
| GET | /api/annotations/mine | 我的所有批注 | 是 |

---

### 模块 6：书评与评分

对读完的书进行评价。

#### 6.1 功能

| 功能 | 说明 |
|------|------|
| 短评 | 一句话评价 + 1-5 星评分 |
| 长评 | 结构化书评（类似 AI 书评的四段式） |
| 评分统计 | 评分分布图（5星占多少、4星占多少...） |
| 书评排序 | 最新、最热、最高分、最低分 |
| AI 辅助写书评 | 一键生成 AI 书评草稿，用户修改后发布 |

#### 6.2 数据库表

```sql
CREATE TABLE reading_reviews (
    review_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    rating TINYINT NOT NULL,
    title VARCHAR(200) DEFAULT '',
    content TEXT,
    is_long_review BOOLEAN DEFAULT FALSE,
    likes_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    UNIQUE KEY uk_review (user_id, book_type, book_id),
    INDEX idx_review_book (book_type, book_id)
);
```

#### 6.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/reviews/:type/:id?page=&sort= | 书评列表 | 否 |
| POST | /api/reviews | 发表书评 | 是 |
| PUT | /api/reviews/:id | 编辑书评 | 是 |
| DELETE | /api/reviews/:id | 删除书评 | 是 |

---

### 模块 7：阅读打卡

培养阅读习惯，记录每日阅读。

#### 7.1 功能

| 功能 | 说明 |
|------|------|
| 自动打卡 | 当天有阅读记录自动视为打卡 |
| 打卡日历 | 日历视图展示打卡记录（绿色标记） |
| 连续天数 | 显示当前连续打卡天数和历史最长 |
| 阅读时长 | 记录每日阅读时长 |
| 打卡分享 | 生成打卡海报图片（复用 SharePoster 组件） |

#### 7.2 数据库表

```sql
CREATE TABLE reading_checkins (
    checkin_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    checkin_date DATE NOT NULL,
    read_minutes INT DEFAULT 0,
    pages_read INT DEFAULT 0,
    books_read INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    UNIQUE KEY uk_checkin (user_id, checkin_date)
);
```

#### 7.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/reading/checkin | 记录阅读（自动打卡） | 是 |
| GET | /api/reading/checkin/calendar?month= | 打卡日历 | 是 |
| GET | /api/reading/checkin/streak | 连续打卡天数 | 是 |

---

### 模块 8：阅读报告

个人中心展示阅读数据 + 年度报告生成。

#### 8.1 个人中心阅读数据

| 功能 | 说明 |
|------|------|
| 本周阅读 | 本周阅读时长、读了几本、多少章 |
| 本月阅读 | 本月阅读数据汇总 |
| 阅读趋势 | 折线图展示近 30 天阅读时长变化 |
| 类型分布 | 饼图展示读过的书籍类型分布 |

#### 8.2 年度阅读报告 `/reading-report`

| 功能 | 说明 |
|------|------|
| 沉浸式报告页 | 全屏滑动卡片，类似网易云年度报告 |
| 核心数据 | 全年阅读字数、书籍数、阅读时长、最晚阅读时间 |
| 阅读之最 | 读过最长的书、读得最快的书、重读最多的书 |
| 类型偏好 | 阅读口味雷达图 |
| 月度曲线 | 12 个月阅读量柱状图 |
| 分享卡片 | 生成可保存/分享的报告图片 |

#### 8.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/reading/report/weekly | 本周数据 | 是 |
| GET | /api/reading/report/monthly | 本月数据 | 是 |
| GET | /api/reading/report/annual | 年度报告 | 是 |
| GET | /api/reading/report/trend?days=30 | 阅读趋势 | 是 |

---

### 模块 9：好句标记与素材联动

阅读中的好句标记同步到写作素材库。

#### 9.1 功能

| 功能 | 说明 |
|------|------|
| 标记好句 | 选中文字 → 标记为好句 |
| 好句列表 | `/highlights` 查看所有标记的好句 |
| 同步素材库 | 一键将好句同步到写作素材库（materials 表） |
| 来源标记 | 素材来源标记为哪本书的哪个段落 |
| 灵感面板集成 | 写作工作室的灵感面板可引用阅读笔记 |

#### 9.2 数据库表

```sql
CREATE TABLE reading_highlights (
    highlight_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_type ENUM('work', 'library') NOT NULL,
    book_id INT NOT NULL,
    chapter_id INT NOT NULL,
    paragraph_idx INT DEFAULT 0,
    selected_text TEXT NOT NULL,
    note VARCHAR(500) DEFAULT '',
    synced_to_material BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE,
    INDEX idx_hl_user (user_id)
);
```

#### 9.3 API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/highlights | 好句列表 | 是 |
| POST | /api/highlights | 标记好句 | 是 |
| DELETE | /api/highlights/:id | 删除标记 | 是 |
| POST | /api/highlights/:id/sync | 同步到素材库 | 是 |

---

### 模块 10：书库管理（后台）

管理员维护书库数据。

| 功能 | 说明 |
|------|------|
| 书籍管理 | 查看、编辑、删除导入的书籍 |
| 批量导入 | 通过脚本批量导入 TXT 文件（增强 import_novel.py） |
| 封面管理 | 上传/生成书籍封面 |
| 标签管理 | 维护分类标签体系 |
| 精选推荐 | 设置书库首页的精选书籍 |

> 此模块优先级低，可在核心功能完成后补充。

---

## 五、页面清单

| 页面 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| 书库首页 | `/library` | 精选推荐、分类浏览、搜索、排行榜 | P0 |
| 书籍详情 | `/library/:type/:id` | 书籍信息、目录、书评、相似推荐 | P0 |
| 沉浸式阅读器 | `/reader/:type/:id` | 统一阅读器（替代/增强现有 Reader.vue） | P0 |
| 个人书架 | `/bookshelf` | 在读/已读/想读/自定义书单 | P1 |
| 我的批注 | `/annotations` | 个人所有批注列表 | P2 |
| 阅读报告 | `/reading-report` | 年度阅读报告（沉浸式） | P2 |
| 书籍上传 | `/library/upload` | 用户上传 TXT 文件 | P1 |
| 我的好句 | `/highlights` | 好句列表、同步素材库 | P2 |

---

## 六、视觉风格规范

| 元素 | 规范 |
|------|------|
| 阅读器背景 | SingularityBackground（speed=0.5, brightness=0.6, saturation=0） |
| 书库/书架页面背景 | 同上，或复用 FluidCursor |
| 卡片 | 毛玻璃 `glass-card` 样式 |
| 主色调 | `--accent-primary: #c4a35a`（金色） |
| 辅助色 | `--accent-purple: #a78bfa` |
| 背景色 | `--bg-primary: #0f0f1a` |
| 字体 | 阅读正文用 `--font-serif`（Noto Serif SC），UI 用 `--font-sans` |

---

## 七、读写联动设计

### 7.1 学习型联动

| 触发点 | 联动方式 |
|------|------|
| 阅读时做批注 | 批注内容可一键发送到写作素材库 |
| 阅读时标记好句 | 好句自动归入"灵感素材"分类 |
| 阅读书评 | AI 书评的"风格分析"可作为写作参考 |

### 7.2 灵感型联动

| 触发点 | 联动方式 |
|------|------|
| 阅读中突发灵感 | 底部"记灵感"按钮，快速记录不离开阅读 |
| 读到精彩情节 | "仿写"按钮，以当前段落为上文调用 AI 续写 |
| 角色对话有趣 | "角色扮演"按钮，跳转到 RP 模块以该角色对话 |

### 7.3 数据型联动

| 触发点 | 联动方式 |
|------|------|
| 阅读时长 | 阅读时长转化为经验值（每 10 分钟 = 1 经验） |
| 阅读打卡 | 连续打卡解锁成就（"书虫"系列成就） |
| 个人中心 | 统一展示写作数据 + 阅读数据的综合面板 |
| 排行榜 | 新增"阅读达人榜"（阅读时长/书籍数） |

---

## 八、技术方案

### 8.1 前端

| 技术点 | 方案 |
|------|------|
| 阅读器背景 | SingularityBackground 组件（已安装，路径 `src/components/ui/bg-singularity/`） |
| 阅读设置 | localStorage 存储，CSS 变量动态切换 |
| 进度记录 | setInterval 定时保存 + beforeunload 事件 |
| 年度报告 | GSAP 动画 + Canvas 生成分享图片 |
| 批注标记 | CSS ::before 伪元素 + 绝对定位标记点 |
| 编码检测 | jschardet 库检测编码（前端 FileReader 读取 ArrayBuffer） |

### 8.2 后端

| 技术点 | 方案 |
|------|------|
| TXT 解析 | 增强 import_novel.py，支持多种章节正则、library_books 入库 |
| 全文搜索 | MySQL LIKE + FULLTEXT 索引 |
| 阅读统计 | 聚合 reading_progress + reading_checkins 数据 |
| 报告生成 | 聚合查询 + AI 生成鼓励文案 |
| 经验值 | 阅读时长写入 writing_sessions，统一计算 |

### 8.3 部署

| 项目 | 说明 |
|------|------|
| 前端路由 | 新增 8 条路由，注册到 router/index.js |
| 后端蓝图 | 新增 8 个 Blueprint，注册到 routes/__init__.py |
| 数据库迁移 | 新增 10 张表，ALTER TABLE 兼容现有数据 |

---

## 九、数据库表汇总

| 表名 | 说明 | 新增/复用 |
|------|------|-----------|
| library_books | 外部导入书籍 | **新增** |
| library_chapters | 外部书籍章节 | **新增** |
| library_volumes | 外部书籍卷 | **新增** |
| reading_bookshelf | 个人书架 | **新增** |
| reading_progress | 阅读进度 | **新增** |
| reading_bookmarks | 书签 | **新增** |
| reading_annotations | 段落批注 | **新增** |
| reading_reviews | 书评评分 | **新增** |
| reading_checkins | 阅读打卡 | **新增** |
| reading_highlights | 好句标记 | **新增** |
| works | 平台作品 | 复用 |
| chapters | 平台章节 | 复用 |
| materials | 写作素材 | 复用（好句同步目标） |
| achievements | 成就定义 | 复用（新增阅读成就） |
| user_achievements | 用户成就 | 复用 |

---

## 十、API 端点汇总（~55 个）

| 蓝图 | 前缀 | 端点数 |
|------|------|--------|
| library_bp | /api/library | 9 |
| bookshelf_bp | /api/bookshelf | 6 |
| reading_bp | /api/reading | 3 |
| bookmarks_bp | /api/bookmarks | 3 |
| annotations_bp | /api/annotations | 6 |
| reviews_bp | /api/reviews | 4 |
| checkin_bp | /api/reading/checkin | 3 |
| report_bp | /api/reading/report | 4 |
| highlights_bp | /api/highlights | 4 |

---

## 十一、实施计划

### 链条 1：导入 → 书库（3-4 天）【P0】

```
TXT 文件解析入库 (library_books + library_chapters)
→ 书库首页 (列表/搜索/分类/排行)
→ 书籍详情页 (信息/目录/操作)
→ 用户上传功能
```

**涉及文件**：
- 新建：`server/routes/library.py`
- 新建：`client/src/views/library/LibraryHome.vue`
- 新建：`client/src/views/library/LibraryDetail.vue`
- 新建：`client/src/views/library/LibraryUpload.vue`
- 修改：`server/database/schema.sql`（新增 3 张表）
- 修改：`server/routes/__init__.py`（注册 library_bp）
- 修改：`client/src/router/index.js`（新增 3 条路由）
- 增强：`server/database/import_novel.py`（支持 library_books 入库）

### 链条 2：书架 → 阅读器（3-4 天）【P0】

```
加入书架 → 书架页面 (在读/已读/想读/书单)
→ 沉浸式阅读器 (滚动/工具栏/章节切换/背景)
→ 阅读设置 (字体/行距/主题)
→ 阅读进度 (自动保存/跨设备同步/续读提示)
```

**涉及文件**：
- 新建：`server/routes/bookshelf.py`
- 新建：`server/routes/reading.py`
- 新建：`client/src/views/library/Bookshelf.vue`
- 重构：`client/src/views/works/Reader.vue` → 统一阅读器
- 修改：`server/database/schema.sql`（新增 2 张表）
- 修改：`server/routes/__init__.py`（注册 2 个蓝图）
- 修改：`client/src/router/index.js`（新增 2 条路由）

### 链条 3：书签 → 批注（2-3 天）【P1】

```
添加书签 → 书签列表/跳转
→ 段落批注 (创建/编辑/公开性切换)
→ 公开批注展示/回复
→ 我的批注列表
```

**涉及文件**：
- 新建：`server/routes/bookmarks.py`
- 新建：`server/routes/annotations.py`
- 新建：`client/src/views/library/Annotations.vue`
- 修改：`client/src/views/works/Reader.vue`（集成书签+批注 UI）
- 修改：`server/database/schema.sql`（新增 2 张表）
- 修改：`server/routes/__init__.py`（注册 2 个蓝图）
- 修改：`client/src/router/index.js`（新增 1 条路由）

### 链条 4：打卡 → 报告（2-3 天）【P1】

```
自动打卡 → 打卡日历/连续天数
→ 阅读时长记录
→ 个人中心阅读数据 (周/月/趋势)
→ 年度阅读报告 (沉浸式/分享卡片)
```

**涉及文件**：
- 新建：`server/routes/checkin.py`
- 新建：`server/routes/report.py`
- 新建：`client/src/views/library/ReadingReport.vue`
- 修改：`client/src/views/user/Profile.vue`（新增阅读数据面板）
- 修改：`server/database/schema.sql`（新增 1 张表）
- 修改：`server/routes/__init__.py`（注册 2 个蓝图）
- 修改：`client/src/router/index.js`（新增 1 条路由）

### 链条 5：好句 → 读写联动（1-2 天）【P2】

```
好句标记 → 同步到素材库
→ 阅读灵感快速记录
→ 经验值/成就联动
→ 导航栏统一入口
```

**涉及文件**：
- 新建：`server/routes/highlights.py`
- 新建：`client/src/views/library/Highlights.vue`
- 修改：`client/src/views/works/Reader.vue`（好句标记 UI）
- 修改：`client/src/components/NavBar.vue`（新增阅读入口）
- 修改：`server/database/schema.sql`（新增 1 张表）
- 修改：`server/routes/__init__.py`（注册 1 个蓝图）
- 修改：`client/src/router/index.js`（新增 1 条路由）

**总计预计：11-16 天**

---

## 十二、验证方案

### 链条 1 验证
1. 用 import_novel.py 导入一本小说到 library_books，确认章节正确
2. 书库首页能看到导入的书 + 平台已发布的作品
3. 点击书籍进入详情页，目录正确显示
4. 用户上传 TXT 文件，自动解析入库

### 链条 2 验证
1. 在书籍详情页点击"加入书架"，书架页面能看到
2. 点击"开始阅读"，进入沉浸式阅读器
3. 调整字体/行距/主题，设置持久化
4. 关闭浏览器重新打开，进度恢复到上次位置

### 链条 3 验证
1. 阅读中添加书签，书签列表能看到
2. 选中文字创建批注，侧边显示标记点
3. 切换批注公开性，其他用户能看到公开批注
4. 回复批注功能正常

### 链条 4 验证
1. 阅读 5 分钟后，打卡日历显示今天已打卡
2. 连续阅读 3 天，连续打卡天数 = 3
3. 个人中心显示本周/本月阅读数据
4. 年度报告页面数据正确

### 链条 5 验证
1. 标记好句，好句列表能看到
2. 点击"同步到素材库"，写作素材库出现该好句
3. 导航栏有"书库"入口
4. 阅读时长正确转化为经验值
