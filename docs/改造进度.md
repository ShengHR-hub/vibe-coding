# 墨池改造进度台账

> 规则见 docs/改造守则.md：每步登记一条，阶段/里程碑结束停靠汇报。状态：✅完成 / 🚧进行 / ⏸停靠。
> 当前优先级（用户 2026-09-03 拍板·v2）：**P1 写作主线强化（W1-W4）→ P2 书籍物理瘦身（R2-R5 延后）→ P3 重造灵感馆（F，UI 统一）**。

## M0 基线整顿 ✅（目标：独立仓库 + 可复现测试基线 + 文档一致）

| 编号 | 目标 | 测试证据 | 状态 |
|---|---|---|---|
| M0.S0 | 全量备份（目录副本 252 文件/58.5MB + git bundle 1.1MB） | 备份完成 | ✅ |
| M0.S1 | 基线捕获：build 绿；pytest 红（根因=测试污染+限速共享，非产品缺陷） | 已记录 | ✅ |
| M0.S2 | 测试隔离：inkstone_test 会话重建 + 每用例清表清限速 | **pytest 48 passed/exit 0**；开发库零污染 | ✅ |
| M0.S3 | 仓库独立：父仓库剔除 + inkstone init | commit 34d8ffb：153 tracked、`.env`/dist/uploads 零入库 | ✅ |
| M0.S4 | 清理遗留（删 test_page.html；敏感/构建物核查） | commit 653a915 | ✅ |
| M0.S5 | 文档同步（README/.env.example/CLAUDE.md/台账） | 数字对齐现实 | ✅ |
| M0.S6 | v-html 统一转义 util（render.js+单测 8/8，替换 5 处重复实现） | commit 428c77f；build 绿 | ✅ |
| M0.S7 | 里程碑验收 | M0 结果摘要见下 | ✅（用户已验收并转向新方向） |

M0 遗留说明：S6 涉及四个渲染面，建议起 dev 后人肉扫一眼；6 个 AI 面板裸 v-html → 已归入 **W1d**。

## 阶段 R1（已完成的入口清理）✅
| 目标 | 状态 |
|---|---|
| 移除书库阅读路由与导航（router/NavBar/UserMenu/Home/Reader），保留 /read/:id 原创作品阅读 | ✅ commit 7f3b285，build 绿 |

## 阶段 W：写作主线强化（当前，P1）
> 计划详见 docs/写作强化计划.md；A-D 全做；可借鉴移植 GitHub 竞品（SillyTavern/WinkNovel/inkos）。

| 编号 | 目标 | 状态 |
|---|---|---|
| W1a | write 9 端点统一按用户限流 + 每日配额（429） | 🚧 进行 |
| W1b | diagnose/summary 输入上限补齐（现只有下限） | ⏸ |
| W1c | prompt 防注入加固（system 提示用户内容视为数据） | ⏸ |
| W1d | 6 个 AI 面板 v-html 统一转义（复用 render.js）+ 会话增长清理 | ⏸ |
| W2a | 续写上下文注入（设定+角色卡 rp_characters+摘要+最近正文，可选 work_id） | ⏸ |
| W2b | 设定记忆 work_lore（轻量 lorebook v1） | ⏸ |
| W3a | 会话管理（新建/切换/清空/删除 UI，先盘点 writing store） | ⏸ |
| W3b | AI 结果落位明确（末尾/替换选中/追加章节） | ⏸ |
| W3c | 导出/收藏一致性 | ⏸ |
| W4a | write 端点 + prompt_builder 支持 references 注入 | ⏸ |
| W4b | 素材/诗词面板"引用到本次创作"（经 writing store） | ⏸ |
| W4c | 每日一句/好句作 references 打通 continue | ⏸ |

## 阶段 R2-R5：书籍物理瘦身（P2，延后）
| 编号 | 目标 | 状态 |
|---|---|---|
| R2 | 注销并删除 10 个书类蓝图 | ⏸ 延后 |
| R3 | schema 删 12 张 library_*/reading_* 表 + 成就/统计口径同步 | ⏸ 延后 |
| R4 | 前端删书类 views/组件（Reader 简化；Profile 清理） | ⏸ 延后 |
| R5 | 文档与测试同步验收 | ⏸ 延后 |
| 备注 | P1/P3 期间 book 蓝图与表**保留不动**，文档口径=已下线（deprecated） | — |

## 阶段 F：重造灵感馆（P3，最后做，UI 与现有风格统一）
| 编号 | 目标 | 状态 |
|---|---|---|
| F1 | 灵感馆统一翻阅入口与浏览体验（复用 materials/poems 数据，不新表） | ⏸ |
| F2 | 素材引用注入的体验面（W4 打通后收口：灵感馆内一键引用/收藏） | ⏸ |
| F3 | 内容录入工具（手动录入；可选爬虫由用户定源） | ⏸ |

## 已作废/归档
- 原 M1"阅读模块收口（54 步计划）"：作废，不执行（`docs/阅读模块实施计划.md` 留档备查）。
- Backlog 中阅读模块缺口清单（段落定位/回复端点/书单等）：随 R 瘦身整体消除，不再逐条修。

## Backlog（保留项）
- [ ] 后端全局安全：Session Cookie SameSite/Secure、CSRF → 写作阶段后专项（P2/P3 间）
- [ ] 零部署资产、无 CI、容器化（仍隔离测试库）→ P3 后
- [ ] 死代码/无用依赖（ui/shader-toy 三重复、three/ogl/lucide/embla 0 引用）→ P3 后瘦身
- [ ] server/test_full.py / test_api.py 冒烟脚本整理 → P3 后
- [ ] Home 页等营销文案仍含"阅读/书库/28 模块"旧口径 → 随 F 阶段统一文案
- [ ] Profile 仍引用 ReadingNav（阅读 Tab）→ R4 处理
