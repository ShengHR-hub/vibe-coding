# 墨池改造进度台账

> 规则见 docs/改造守则.md：每步登记一条，里程碑结束停靠汇报。状态：✅完成 / 🚧进行 / ⏸停靠。

## M0 基线整顿（目标：独立仓库 + 可复现测试基线 + 文档一致）

| 编号 | 目标 | 主要文件 | 测试证据 | 状态 |
|---|---|---|---|---|
| M0.S0 | 全量备份 | backups/inkstone-20260903-204208（252 文件/58.5MB）+ vibe-coding bundle 1.1MB | 备份完成 | ✅ |
| M0.S1 | 基线捕获 | — | 前端 `npm run build` ✅；pytest 基线红（11f/23p/14e，根因=测试自身污染与限速共享，非产品缺陷） | ✅ |
| M0.S2 | 测试隔离 | server/tests/conftest.py | pytest **48 passed / exit 0**；开发库 inkstone 零污染（users 仍 26），inkstone_test 37 表 | ✅ |
| M0.S3 | 仓库独立 | 父仓库剔除 + inkstone `.git` init | commit 34d8ffb：153 tracked、状态干净、`.env`/dist/uploads 零入库 | ✅ |
| M0.S4 | 清理遗留 | server/test_page.html（删除）；敏感/构建物核查 | commit 653a915；`ls-files` 无 uploads/pyc/dist/.env/logs | ✅ |
| M0.S5 | 文档同步 | README.md / server/.env.example / CLAUDE.md / 本台账 | 静态核对数字（37 表/27 蓝图/42 页面/150+ 端点） | 🚧 |
| M0.S6 | v-html 统一转义 util | client/src/utils/render.js + 各视图 + util 单测 | 待跑：node --test + npm run build | ⏸ |
| M0.S7 | 里程碑验收 | — | 停靠汇报，等用户确认后进 M1 | ⏸ |

## Backlog（发现但按纪律不顺手改）

- [ ] 6 个 AI 面板裸 `v-html="item.text"` 直接渲染模型输出（XSS 面）→ 计划 M2 专项处理（CharacterPanel/ContinuePanel/InspirePanel/OutlinePanel/PoemPanel?/PolishPanel/PromptPanel）
- [ ] AI 9 端点无限流、diagnose/summary 无输入上限 → M2
- [ ] 阅读模块 54 步缺口清单（书签/批注段落级定位恒 0、无回复端点、6 死端点、书单不落库等）→ M1
- [ ] Session Cookie SameSite/Secure、CSRF → M2
- [ ] 零部署资产、无 CI、测试依赖真实 MySQL（已隔离但需容器化）→ M3
- [ ] 死代码/无用依赖（ui/shader-toy 三重复、three/ogl/lucide/embla 0 引用等）→ M3 或专项清理
- [ ] server/test_full.py、test_api.py 为 localhost 冒烟脚本（非 pytest）→ M3 整理
