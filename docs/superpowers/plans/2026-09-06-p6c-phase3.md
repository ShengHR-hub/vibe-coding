# 三期 P6-C 计划：成书工作流收口

> 阶段②（目标 goal-9d48af35）。遵守纪律：每批独立 commit + 全量测试绿（pytest + vitest + build）才进下一批。

## 范围（登记台账 P6-C）

| 批次 | 内容 | 验收 |
|---|---|---|
| C1 | **章节草稿/正式状态 + 交付只导正式稿**：chapters 加 status(draft/formal)；保存章节带状态；写台/新手页章节侧栏状态标识 + 标记正式稿；导出加 `?formal=1` 只导正式章；FinalizePanel 交付按钮用正式稿 | pytest 129+新用例 绿 / vitest 绿 / build 绿 |
| C2 | **角色卡 AI 生成/刷新/手改**：复用 rp_characters（既有表+提取 API）；补角色卡手改 PUT /api/rp/characters/<id>；新增 AI 按灵感生成角色卡（入 rp_characters）；写台「角色设定」工具升级为角色卡管理（生成/刷新/手改列表） | pytest 补用例 绿 / build 绿 |
| C3 | **角色关系图（手动更新）**：新表 work_relations（work_id/source/relation/target）；API GET/POST/DELETE；写台或作品页关系图维护入口（列表+连线编辑，手填关系）；AI 可辅助生成草稿 | pytest 补用例 绿 / build 绿 |
| C4 | **结构审校 + 内容诊断合并入口**：一个入口（写台右侧审查段）整合 StructurePanel 统计自查 + DiagnosePanel 文字诊断两步走 | pytest 全量绿 / build 绿 |

## 边界约束（沿用）

- 不新建业务表（C3 关系图属需持久化的新语义，允许开新表 work_relations——schema.sql 明确定义，测试库自动重建）
- 权限隔离：所有新端点校验 work 归属 user_id；导出沿用 owner 校验
- 字段语义不漂移：chapters.status 只表达 草稿/正式稿；export 的 formal 参数只过滤章节状态

## 测试纪律

- 后端：`cd server && python -m pytest tests -q -p no:cacheprovider`，隔离库 inkstone_test 自动从 schema.sql 重建
- 前端：`cd client && npm test`（vitest，阶段①基建）；交互冒烟需用户浏览器验证并注明路径
- 每批结束更新 `docs/改造进度.md`