# 墨池 Inkstone · 上线记录（2026-09-04）

## 访问地址
- **https://ink.shenghui2026.top**（生产正式入口，HTTP 自动跳转 HTTPS）
- 备案：域名 shenghui2026.top 已在阿里云备案，主域与子域均可使用

## 运行环境（借用于舍友服务器，请勿干扰其服务）
- 阿里云轻量应用服务器 · 北京（`82.156.254.168`，实例 lhins-2fyt8ack）
- Ubuntu 22.04 · 2C2G · 50GB · 300GB/月流量；已启用 1GB swap
- 端口占用：80/443 = 系统 Nginx（含舍友 shenghui2026.top 站点与我方 ink 站点）；8080 = 舍友 Java 服务
- Docker 26 已装但本项目**未用 Docker**（原生 gunicorn，便于与其现有环境共存）

## 我方部署结构
| 组件 | 位置 |
|---|---|
| 代码与前端 dist | `/srv/inkstone`（上传自本仓库打包 tgz） |
| Python 环境 | `/srv/inkstone/server/.venv` |
| 生产配置 | `/srv/inkstone/server/.env`（SECRET_KEY/MySQL/MiMo，chmod 600） |
| 后端服务 | systemd `inkstone.service`（gunicorn 2 worker → 127.0.0.1:8000，开机自启） |
| Nginx | `/etc/nginx/conf.d/ink-inkstone.conf`（`server_name ink.shenghui2026.top`，SSE 关缓冲） |
| HTTPS | Let's Encrypt 证书（/opt/certbot 独立 venv 签发，自动续期由 systemd timer 接管） |
| 数据库 | MySQL 8 本地库 `inkstone`，应用账号 `inkstone`@localhost（密码存于 server/.env） |

## 常用运维命令（ssh: ubuntu@82.156.254.168）
```bash
sudo systemctl status inkstone     # 服务状态/日志
sudo journalctl -u inkstone -f     # 实时日志
sudo systemctl restart inkstone    # 改代码/配置后重启
sudo nginx -t && sudo systemctl reload nginx
sudo /opt/certbot/bin/certbot renew --dry-run   # 证书续期检查
sudo mysql --defaults-file=/etc/mysql/debian.cnf -e "SHOW DATABASES;"
```

## 版本更新步骤
1. 本地打包：在仓库根打包（排除 .git/node_modules/results/env/logs）得到 tgz；
2. `scp` 上传到服务器 /tmp；
3. `sudo tar -xzf /tmp/inkstone.tgz -C /srv/inkstone`（覆盖代码与前端 dist）；
4. `sudo systemctl restart inkstone`；
5. 涉及数据库结构变化时，先按变更执行幂等 SQL（schema.sql 变更需人工在库执行，勿整体重灌，避免清空生产数据）。

## 数据迁移（2026-09-04 已执行）
- 线上库已由"本地开发库"迁移而来：删除 11 个测试/垃圾账号（testuser/AAA 等），**保留李白（id3）与文豪种子作者号（共 16 用户）**，作品 27 部、诗词/素材/对话等全局数据全部保留；
- 李白为对外主账号（使用其原密码登录）；种子作者号不可登录仅作演示内容；
- 说明：迁移的作品多为**草稿状态**，在"我的作品"里发布后才会出现在广场/公开页；
- 本地原开发库 inkstone 未改动，另存备份包 `migrate_inkstone.sql`（仓库外文件，勿提交）。

## 上线自检（已通过 2026-09-04）
- [x] https://ink.shenghui2026.top 打开墨池首页（200，含"墨池"）
- [x] /api/health 返回 {"code":0,...}
- [x] 证书 CN=ink.shenghui2026.top，有效期至 2026-12-03
- [ ] 待办：注册首个正式账号并冒烟（AI 功能走 server/.env 的 MiMo key，注意配额）
